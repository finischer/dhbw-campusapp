import { StyleSheet } from "react-native";
export const appInfoStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 10,
  },
  text: {
    opacity: 0.2,
  },
});
