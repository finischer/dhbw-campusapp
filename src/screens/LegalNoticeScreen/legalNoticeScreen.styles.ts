import { StyleSheet } from "react-native";
import { SPACING } from "../../constants/layout";

export const legalNoticeScreenStyles = StyleSheet.create({
  contactRowContainer: {
    alignItems: "center",
    flexDirection: "row",
  },
  contactRowText: {
    marginLeft: SPACING.sm,
  },
  contactContainer: {},
});
