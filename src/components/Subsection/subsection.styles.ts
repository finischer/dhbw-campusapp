import { SPACING } from "../../constants/layout/layout";
import { StyleSheet } from "react-native";
export const subsectionStyles = StyleSheet.create({
  wrapperContainer: {
    marginBottom: SPACING.m,
  },
  titleContainer: {
    paddingBottom: SPACING.s,
  },
});
