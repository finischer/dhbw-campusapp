import { StyleSheet } from "react-native";
export const errorViewStyles = StyleSheet.create({
  text: {
    textAlign: "center",
  },
  retryButton: {},
  buttonsContainer: {
    marginTop: 30,
    flexDirection: "row",
    alignItems: "center",
  },
});
